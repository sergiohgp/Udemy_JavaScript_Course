let myBook = {
    title: '1984',
    author: 'Geaorge Orwell',
    pageCount: 326
}

console.log(`${myBook.title} by ${myBook.author}`)

myBook.title = 'Animal Farm'

console.log(`${myBook.title} by ${myBook.author}`)

let person = {
    name: 'Sergio',
    age: 28,
    location: 'Toronto'
}

console.log(person)
console.log(`${person.name} id ${person.age} and lives in ${person.location}`)